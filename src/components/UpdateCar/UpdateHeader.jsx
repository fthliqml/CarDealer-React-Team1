import { CardHeader, CardTitle } from "../ui/card";

const UpdateHeader = () => {
  return (
    <CardHeader>
      <CardTitle className={"flex justify-center"}>Update Car</CardTitle>
    </CardHeader>
  );
};

export default UpdateHeader;
